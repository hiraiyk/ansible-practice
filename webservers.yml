---
# file: webservers.yml
- hosts: webservers
  remote_user: jenkins
  become: yes
  become_user: root
  ansible_become_password: intellilink
  gather_facts: yes
  roles:
    - common
    - nginx
#    - logstash
